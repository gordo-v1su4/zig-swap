# Archived Rust spike (V1S-73)

Comparison harness only. **Zig selected** — see `docs/adr/0008-zig-compiled-core.md`.

Kept for reference. Not part of the v0 build path.

```bash
cargo test --manifest-path core/archive/rust-spike/Cargo.toml

Fixtures path: `core/fixtures/` (via `../../fixtures` from this crate).
```
